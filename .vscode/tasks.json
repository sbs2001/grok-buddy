{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "My Task",
            "command": "${command:workbench.action.toggleSidebarVisibility}",
            "runOptions": {
                "runOn": "folderOpen"
            }
        }
    ]
}